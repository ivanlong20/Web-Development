<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="chicken" name="choice">🍗</button>
      <button type="submit" value="beef" name="choice">🥩</button>
      <button type="submit" value="fish" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" class="hidden">
      <!-- If no recipe yet, show: -->
      <% if(!locals.recipe){ %>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
      <% } else if (locals.selection === "chicken"){ %>
      <h2><%= recipe[0].name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%=recipe[0].ingredients.protein.name %>,
          <%=recipe[0].ingredients.protein.preparation %>
        </li>
        <li><%=recipe[0].ingredients.salsa.name %></li>
        <% recipe[0].ingredients.toppings.forEach(element => {%>
        <li><%=element.quantity %> of <%=element.name%></li>
        <%}); %>
      </ul>
      <% } else if (locals.selection === "beef"){ %>
      <h2><%= recipe[1].name %></h2>

      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%=recipe[1].ingredients.protein.name %>,
          <%=recipe[1].ingredients.protein.preparation %>
        </li>
        <li><%=recipe[1].ingredients.salsa.name %></li>
        <% recipe[1].ingredients.toppings.forEach(element => {%>
        <li><%=element.quantity %> of <%=element.name%></li>
        <%}); %>
      </ul>
      <% } else if (locals.selection === "fish"){ %>
      <h2><%= recipe[2].name %></h2>

      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%=recipe[2].ingredients.protein.name %>,
          <%=recipe[2].ingredients.protein.preparation %>
        </li>
        <li><%=recipe[2].ingredients.salsa.name %></li>
        <% recipe[2].ingredients.toppings.forEach(element => {%>
        <li><%=element.quantity %> of <%=element.name%></li>
        <%}); %>
      </ul>
      <% } %>
    </div>
  </body>
</html>
